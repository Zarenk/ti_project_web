"use client"

import type { ProductDetailProps } from "../types"

export default function ClassicProductDetail(_props: ProductDetailProps) {
  // Placeholder — the product detail page is currently a monolithic component.
  // This will be expanded when the page is refactored (Phase 5).
  return null
}
